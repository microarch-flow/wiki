# 输入编码与权重表示

## 需要统一记录的维度

- 输入是 `bit-serial` 还是 `bit-parallel`
- 输入是电压、电流、脉宽还是数字码
- 权重是 binary、ternary、INT4、INT8 还是 multi-level cell
- partial sum 在哪里生成和合并

## 这页的用途

后续读论文时，可以把每篇设计的编码方式先落到这一页的统一框架里，方便横向比较。

## 可补充内容

- 编码方式对精度和吞吐的影响
- 不同表示方法的面积代价
- 典型设计对照表
